---
phase: 02-lead-capture-and-automation
plan: 04
type: execute
wave: 3
depends_on: [02-02, 02-03]
files_modified: []
autonomous: false
requirements: [AUTO-01, AUTO-02, AUTO-03]

must_haves:
  truths:
    - "Family receives SMS within 60 seconds of form submission on the live system"
    - "Family receives email within 60 seconds of form submission on the live system"
    - "Make.com scenario shows at least 3 scheduled follow-up touchpoints after initial contact"
  artifacts:
    - path: "app/[lccSlug]/page.tsx"
      provides: "Live landing page reachable at staging/prod URL"
    - path: "app/api/leads/[id]/route.ts"
      provides: "Make.com can call this endpoint to fetch lead data"
  key_links:
    - from: "Make.com scenario"
      to: "GET /api/leads/[id]"
      via: "HTTP module in Make.com scenario fetches lead data on trigger"
      pattern: "manual"
    - from: "Make.com scenario"
      to: "Twilio SMS module"
      via: "SMS send within first scenario step"
      pattern: "manual"
---

<objective>
Verify the full end-to-end automation: a real form submission on the deployed application triggers Make.com, which sends an SMS and email within 60 seconds, with the nurture sequence showing at least 3 follow-up touchpoints.

Purpose: AUTO-01, AUTO-02, AUTO-03 cannot be automated in local tests — they require live Make.com scenarios and Twilio A2P 10DLC credentials. This checkpoint captures the manual verification.
Output: Confirmed: speed-to-lead under 60 seconds, email delivered, nurture sequence configured with 3+ touchpoints.
</objective>

<execution_context>
@/Users/eliahshansult/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eliahshansult/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-lead-capture-and-automation/02-CONTEXT.md
@.planning/phases/02-lead-capture-and-automation/02-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify live Make.com automation — SMS, email, nurture sequence</name>
  <action>No code changes. This checkpoint verifies that the Make.com scenario is configured and live automation works end-to-end.</action>
  <verify>
    <automated>MISSING — AUTO-01, AUTO-02, AUTO-03 require live Make.com + Twilio credentials; no automated test possible</automated>
  </verify>
  <done>Family receives SMS and email within 60 seconds of form submission; Make.com scenario shows 3+ nurture touchpoints.</done>
  <what-built>
Full Phase 2 implementation is complete:
- Public landing page at /[lccSlug] with TCPA consent form
- Server action creating leads in Supabase and firing Make.com webhook
- GET /api/leads/[id] endpoint for Make.com to fetch lead personalization data
- POST /api/leads/[id]/callback endpoint for Make.com to update pipeline stage and trigger referral

All automated tests pass: `npx playwright test lead-capture/` and `npx vitest run tests/integration/`
  </what-built>
  <how-to-verify>
**Prerequisites before verifying:**
1. Phase 2 migration applied to Supabase (supabase/migrations/20260315000000_phase2_lead_capture.sql)
2. MAKE_WEBHOOK_SECRET set in .env.local and matching the secret configured in Make.com scenario HTTP header
3. Twilio A2P 10DLC registration complete (brand + campaign approved) — this is an ops prerequisite, not a code task
4. Make.com scenario configured with:
   - Trigger: Custom webhook receiving `{ leadId }` payload
   - Step 1: HTTP GET to `https://[your-domain]/api/leads/[leadId]` with `Authorization: Bearer [MAKE_WEBHOOK_SECRET]` header
   - Step 2: Twilio SMS send to lead's phone number (within 60 seconds of trigger)
   - Step 3: Email send to lead's email (within 60 seconds of trigger)
   - Steps 4+: At minimum 3 scheduled follow-up touchpoints (e.g., Day 2 SMS, Day 4 email, Day 7 SMS)
   - LCC row in Supabase has webhook_url set to this Make.com scenario's webhook URL

**Verification steps:**
1. Visit `https://[your-domain]/[lcc-slug]` without being logged in — confirm page loads with LCC name and form
2. Submit the form with a real test phone number and email address
3. Within 60 seconds: confirm the test phone receives an SMS from the LCC's Twilio number
4. Within 60 seconds: confirm the test email inbox receives an email
5. Check Supabase leads table: confirm the new lead row has stage = "Interested" and all fields populated (phone, consent_text, consent_ip, etc.)
6. Check Make.com scenario execution history: confirm it ran successfully and called GET /api/leads/[leadId]
7. Submit the same form again with the same email — confirm no duplicate lead row is created in Supabase and no second SMS/email fires
8. In Make.com scenario editor: confirm at least 3 follow-up touchpoints are scheduled after the initial send
9. (Optional) Manually set a lead's stage to "Signed" via Supabase dashboard — confirm Make.com referral scenario fires (if referral_webhook_url is configured for the LCC)
  </how-to-verify>
  <resume-signal>Type "approved" when speed-to-lead SMS and email are confirmed within 60 seconds and nurture sequence shows 3+ touchpoints. Type "issues: [description]" if anything fails.</resume-signal>
</task>

</tasks>

<verification>
Manual verification only — see checkpoint task above.

AUTO-01, AUTO-02, AUTO-03 are marked as manual-only in VALIDATION.md because they require:
- Live Make.com scenario with active Twilio A2P 10DLC registration
- Real SMS delivery to a physical phone
- Email delivery to a real inbox
- Days-long nurture sequence timing (cannot be automated in tests)
</verification>

<success_criteria>
- Family receives SMS within 60 seconds of form submission (AUTO-01)
- Family receives email within 60 seconds of form submission (AUTO-02)
- Make.com scenario shows at least 3 scheduled follow-up touchpoints (AUTO-03)
- No duplicate lead created on second form submission with same email
</success_criteria>

<output>
After completion, create `.planning/phases/02-lead-capture-and-automation/02-04-SUMMARY.md`
</output>
