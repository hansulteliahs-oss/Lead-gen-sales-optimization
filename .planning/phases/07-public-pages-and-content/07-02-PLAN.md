---
phase: 07-public-pages-and-content
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260407000000_phase7_kim_seed.sql
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-03

must_haves:
  truths:
    - "kim-johnson lccs row has non-null headline, subheadline, bio, and bio_teaser after migration runs"
    - "At least 3 testimonials exist in lcc_testimonials for kim-johnson"
    - "At least 5 FAQ entries exist in lcc_faqs for kim-johnson"
    - "Migration is idempotent — running it twice does not create duplicate testimonials/FAQs"
    - "photo_url remains NULL — pages must handle null gracefully"
  artifacts:
    - path: "supabase/migrations/20260407000000_phase7_kim_seed.sql"
      provides: "SQL migration seeding Kim's headline, subheadline, bio, bio_teaser, testimonials, FAQs"
      contains: "UPDATE public.lccs"
  key_links:
    - from: "supabase/migrations/20260407000000_phase7_kim_seed.sql"
      to: "public.lccs WHERE slug = 'kim-johnson'"
      via: "UPDATE statement"
      pattern: "UPDATE.*lccs.*slug.*kim-johnson"
    - from: "supabase/migrations/20260407000000_phase7_kim_seed.sql"
      to: "public.lcc_testimonials"
      via: "INSERT with subquery for lcc_id"
      pattern: "INSERT INTO.*lcc_testimonials"
    - from: "supabase/migrations/20260407000000_phase7_kim_seed.sql"
      to: "public.lcc_faqs"
      via: "INSERT with subquery for lcc_id"
      pattern: "INSERT INTO.*lcc_faqs"
---

<objective>
Write and apply the Kim Johnson seed migration. This is a single SQL file that updates kim-johnson's website copy and inserts placeholder testimonials and FAQs — all AI-drafted, ready for a real operator swap before going live.

Purpose: Turn CONT-01, CONT-02, CONT-03 GREEN. The kim-seed.test.ts integration tests (Plan 01) will pass after this migration is applied.
Output: One SQL migration file applied to Supabase.
</objective>

<execution_context>
@/Users/eliahshansult/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eliahshansult/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-public-pages-and-content/07-CONTEXT.md
@.planning/phases/07-public-pages-and-content/07-RESEARCH.md

<interfaces>
<!-- Existing migration pattern — follow exactly -->

Latest migration: supabase/migrations/20260405000000_phase6_website_infra.sql
Schema confirmed:
  public.lccs — has columns: id, slug, name, headline, subheadline, bio, bio_teaser, photo_url (nullable)
  public.lcc_testimonials — id (uuid default), lcc_id (FK to lccs.id), family_name (text), quote (text), order_index (int), created_at
  public.lcc_faqs — id (uuid default), lcc_id (FK to lccs.id), question (text), answer (text), order_index (int), created_at

kim-johnson row already exists in public.lccs (inserted in Phase 6 E2E test setup / seed).

Idempotency pattern from existing migrations:
  Use ON CONFLICT DO NOTHING for inserts, or DELETE + re-INSERT wrapped in a transaction.
  For this seed migration: delete existing kim-johnson testimonials/faqs before inserting
  to make the migration re-runnable without duplicates.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Kim seed migration and apply to Supabase</name>
  <files>supabase/migrations/20260407000000_phase7_kim_seed.sql</files>
  <action>
    Create `supabase/migrations/20260407000000_phase7_kim_seed.sql` with three sections:

    **CONT-01: UPDATE kim-johnson's website copy**
    UPDATE public.lccs SET headline, subheadline, bio_teaser, bio, photo_url=NULL WHERE slug='kim-johnson'.

    Headline: warm, professional, trust-building for a Local Childcare Consultant.
    Suggested: "Your Local Guide to Finding the Perfect Au Pair"

    Subheadline (1 sentence): "I help families across the area discover the au pair program — a childcare solution that combines flexibility, cultural connection, and real affordability."

    bio_teaser (2–3 sentences for landing page about teaser):
    "As a certified Local Childcare Consultant with Cultural Care Au Pair, I've spent years guiding families through the process of welcoming an au pair into their home. From your first question to your au pair's arrival, I'm here every step of the way — making a complex process feel personal and simple."

    bio (full paragraphs for /about page — 2–3 paragraphs):
    Include: background in childcare/family services, years as an LCC, what she loves about the work, personal touch (family life, community involvement). Write in first or third person — Claude's discretion. Warm and trustworthy tone. Use \n\n for paragraph breaks.

    photo_url: explicitly set to NULL.

    **CONT-02: Seed at least 3 testimonials for kim-johnson**
    Use DELETE before INSERT for idempotency:
    ```sql
    DELETE FROM public.lcc_testimonials
    WHERE lcc_id = (SELECT id FROM public.lccs WHERE slug = 'kim-johnson');
    ```
    Then INSERT 3 testimonials with order_index 0, 1, 2. Use subquery `(SELECT id FROM public.lccs WHERE slug = 'kim-johnson')` as lcc_id — never a hardcoded UUID.

    Testimonial copy (AI-drafted, family-facing, realistic):
    - family_name: "The Martinez Family", quote: warm, specific, mentions Kim by name, ~2 sentences
    - family_name: "Sarah T.", quote: short, punchy, mentions ease of process or Kim's responsiveness
    - family_name: "The Nguyen Family", quote: mentions cultural exchange or how au pair fit their life

    **CONT-03: Seed at least 5 FAQ entries for kim-johnson**
    DELETE before INSERT (same idempotency pattern):
    ```sql
    DELETE FROM public.lcc_faqs
    WHERE lcc_id = (SELECT id FROM public.lccs WHERE slug = 'kim-johnson');
    ```
    Then INSERT at least 5 FAQs with order_index 0–4. Use subquery for lcc_id.

    FAQ topics required per CONTEXT.md (all family-facing):
    1. How much does the au pair program cost? (order_index 0)
    2. How long does the matching process take? (order_index 1)
    3. What does a typical living arrangement look like? (order_index 2)
    4. What happens if the au pair isn't the right fit? (order_index 3)
    5. Does the au pair need a visa, and who handles it? (order_index 4) — visa question deferred to FAQ
    Optional 6th: What's the difference between an au pair and a nanny? (order_index 5)

    Answers: 2–4 sentences each. Accurate to the Cultural Care au pair program. Professional, reassuring tone.

    **Apply the migration:**
    After writing the file, run:
    ```bash
    npx supabase db push --linked
    ```
    This applies the migration to the linked remote Supabase project.
    If `db push` fails, try: `npx supabase migration up --linked`
  </action>
  <verify>
    <automated>npx vitest run tests/integration/kim-seed.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    Migration file exists at supabase/migrations/20260407000000_phase7_kim_seed.sql.
    `npx vitest run tests/integration/kim-seed.test.ts` passes all 3 tests (GREEN).
    kim-johnson lccs row has non-null headline, subheadline, bio, bio_teaser.
    At least 3 testimonials and 5 FAQs exist for kim-johnson in the DB.
  </done>
</task>

</tasks>

<verification>
Integration tests green after migration:

```bash
npx vitest run tests/integration/kim-seed.test.ts
```

Expected: 3 passing tests covering CONT-01, CONT-02, CONT-03.
</verification>

<success_criteria>
- Migration file created with correct filename (20260407000000_phase7_kim_seed.sql)
- Migration applied to remote Supabase (db push succeeds)
- kim-seed.test.ts passes: headline/subheadline/bio/bio_teaser non-null, 3+ testimonials, 5+ FAQs
- Migration is idempotent (DELETE before INSERT prevents duplicate rows)
- photo_url is NULL (graceful fallback tested in page renders)
</success_criteria>

<output>
After completion, create `.planning/phases/07-public-pages-and-content/07-02-SUMMARY.md`
</output>
