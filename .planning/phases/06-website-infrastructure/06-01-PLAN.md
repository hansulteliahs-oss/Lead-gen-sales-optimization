---
phase: 06-website-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260405000000_phase6_website_infra.sql
  - tests/integration/website-infra.test.ts
autonomous: true
requirements: [SITE-01, SITE-02, SITE-03, SITE-04]

must_haves:
  truths:
    - "The lccs table has headline, subheadline, bio, bio_teaser, photo_url, and custom_domain columns"
    - "The lcc_testimonials table exists with id, lcc_id FK, family_name, quote, order_index, created_at"
    - "The lcc_faqs table exists with id, lcc_id FK, question, answer, order_index, created_at"
    - "The lcc-photos Storage bucket exists with public read access"
  artifacts:
    - path: "supabase/migrations/20260405000000_phase6_website_infra.sql"
      provides: "Schema extensions and storage bucket"
      contains: "ALTER TABLE public.lccs"
    - path: "tests/integration/website-infra.test.ts"
      provides: "Integration tests for SITE-01 through SITE-04"
      exports: []
  key_links:
    - from: "supabase/migrations/20260405000000_phase6_website_infra.sql"
      to: "public.lccs"
      via: "ALTER TABLE adding 6 nullable columns"
      pattern: "ADD COLUMN IF NOT EXISTS"
    - from: "supabase/migrations/20260405000000_phase6_website_infra.sql"
      to: "public.lcc_testimonials"
      via: "CREATE TABLE with FK to lccs.id"
      pattern: "REFERENCES public.lccs"
    - from: "supabase/migrations/20260405000000_phase6_website_infra.sql"
      to: "storage.buckets"
      via: "INSERT for lcc-photos bucket"
      pattern: "INSERT INTO storage.buckets"
---

<objective>
Extend the database schema with website content columns, create testimonials and FAQs tables, and provision the lcc-photos Storage bucket — all in a single idempotent migration file. Write integration tests that verify the schema and bucket exist.

Purpose: Phase 7 page components need these columns and tables to render content. The storage bucket is needed for LCC profile photos. Without this migration, no website content can be stored.
Output: One migration SQL file, one integration test file.
</objective>

<execution_context>
@/Users/eliahshansult/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eliahshansult/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-website-infrastructure/06-RESEARCH.md

<interfaces>
<!-- Existing migration patterns from Phase 1 -->
From supabase/migrations/20260314000000_foundation.sql:
- Uses `CREATE TABLE IF NOT EXISTS`, `CREATE INDEX IF NOT EXISTS`
- RLS policies wrapped in `DO $$ BEGIN IF NOT EXISTS ... END $$;` for idempotency
- Tables use `UUID PRIMARY KEY DEFAULT gen_random_uuid()`, `TIMESTAMPTZ NOT NULL DEFAULT NOW()`
- FK pattern: `REFERENCES public.lccs(id) ON DELETE CASCADE`

From utils/supabase/admin.ts:
```typescript
export function createAdminClient()
// Returns Supabase client with service role key (bypasses RLS)
```

From tests/integration/lead-upsert.test.ts (existing test pattern):
- Uses Vitest with `import { describe, it, expect } from 'vitest'`
- Uses `createAdminClient()` for DB assertions
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write integration test stubs (Wave 0 RED)</name>
  <files>tests/integration/website-infra.test.ts</files>
  <behavior>
    - SITE-01: Querying lccs table for columns headline, subheadline, bio, bio_teaser, photo_url, custom_domain does not error (columns exist)
    - SITE-02: Inserting and querying lcc_testimonials with lcc_id FK succeeds; table has id, lcc_id, family_name, quote, order_index, created_at
    - SITE-03: Inserting and querying lcc_faqs with lcc_id FK succeeds; table has id, lcc_id, question, answer, order_index, created_at
    - SITE-04: Querying storage.buckets for id='lcc-photos' returns a row with public=true
  </behavior>
  <action>
    Create `tests/integration/website-infra.test.ts` with 4 test cases — one per SITE requirement.

    Use `createAdminClient()` from `@/utils/supabase/admin` (same pattern as `tests/integration/lead-upsert.test.ts`).

    SITE-01 test: Select `headline, subheadline, bio, bio_teaser, photo_url, custom_domain` from `lccs` table with a `.limit(1)` query. Assert no Postgres error (error is null). This proves the columns exist — they will be null but queryable.

    SITE-02 test: Insert a test row into `lcc_testimonials` using Kim's lcc_id (from seed data — query `lccs` by slug `kim-johnson` first). Assert insert succeeds. Then select back and verify `family_name`, `quote`, `order_index` are present. Clean up the test row after.

    SITE-03 test: Same pattern as SITE-02 but for `lcc_faqs` — insert a test row with `question` and `answer`, verify it reads back, clean up.

    SITE-04 test: Use admin client to query `storage.buckets` table via `.from('buckets')` on a storage-schema client. Actually, the simpler approach: use `supabase.storage.getBucket('lcc-photos')` and assert it returns data with `public: true` and no error.

    All tests should be real tests (not skipped stubs) — they will fail RED until the migration runs. Mark with a comment: `// RED until migration 20260405000000 applied`.
  </action>
  <verify>
    <automated>cd /Users/eliahshansult/AI\ Work/Claude\ Code/lcc-lead-engine && npx vitest run tests/integration/website-infra.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>Test file exists with 4 test cases. Tests fail (RED) because migration has not been applied yet.</done>
</task>

<task type="auto">
  <name>Task 2: Create Phase 6 migration SQL</name>
  <files>supabase/migrations/20260405000000_phase6_website_infra.sql</files>
  <action>
    Create a single migration file at `supabase/migrations/20260405000000_phase6_website_infra.sql` containing all schema changes. Use the exact SQL patterns from the research document (Pattern 3), which mirrors Phase 1 migration conventions.

    The migration must contain these 5 sections in order:

    **Section 1 — Extend lccs table:**
    `ALTER TABLE public.lccs ADD COLUMN IF NOT EXISTS` for each of: `headline TEXT`, `subheadline TEXT`, `bio TEXT`, `bio_teaser TEXT`, `photo_url TEXT`, `custom_domain TEXT`. All nullable — no NOT NULL constraints (LCCs without website content should not break).

    **Section 2 — Create lcc_testimonials table:**
    `CREATE TABLE IF NOT EXISTS public.lcc_testimonials` with columns: `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`, `lcc_id UUID NOT NULL REFERENCES public.lccs(id) ON DELETE CASCADE`, `family_name TEXT NOT NULL`, `quote TEXT NOT NULL`, `order_index INTEGER NOT NULL DEFAULT 0`, `created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`. Enable RLS. Create index on `lcc_id`.

    **Section 3 — Create lcc_faqs table:**
    `CREATE TABLE IF NOT EXISTS public.lcc_faqs` with columns: `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`, `lcc_id UUID NOT NULL REFERENCES public.lccs(id) ON DELETE CASCADE`, `question TEXT NOT NULL`, `answer TEXT NOT NULL`, `order_index INTEGER NOT NULL DEFAULT 0`, `created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`. Enable RLS. Create index on `lcc_id`.

    **Section 4 — RLS policies:**
    Public read (SELECT) for both `anon` and `authenticated` roles on `lcc_testimonials` and `lcc_faqs` using `USING (true)`. Wrap in `DO $$ BEGIN IF NOT EXISTS ... END $$;` for idempotency (same pattern as Phase 1 foundation migration).

    **Section 5 — Storage bucket:**
    `INSERT INTO storage.buckets (id, name, public) VALUES ('lcc-photos', 'lcc-photos', true) ON CONFLICT (id) DO NOTHING;`
    Then create a public read policy on `storage.objects` for `bucket_id = 'lcc-photos'` — same idempotent `DO $$ BEGIN IF NOT EXISTS ... END $$;` wrapper.

    After writing the file, apply it: `npx supabase db reset` (this re-applies all migrations including the new one). Then re-run the integration tests to verify GREEN.
  </action>
  <verify>
    <automated>cd /Users/eliahshansult/AI\ Work/Claude\ Code/lcc-lead-engine && npx supabase db reset --linked 2>&1 | tail -5 && npx vitest run tests/integration/website-infra.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>Migration applied successfully. All 4 integration tests pass GREEN: lccs columns exist, lcc_testimonials and lcc_faqs tables work with FK constraints, lcc-photos bucket exists and is public.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/integration/website-infra.test.ts` — all 4 tests pass
- `npx supabase db reset --linked` completes without errors (migration is idempotent)
</verification>

<success_criteria>
- lccs table has 6 new nullable columns (headline, subheadline, bio, bio_teaser, photo_url, custom_domain)
- lcc_testimonials table exists with correct schema, FK, RLS, and index
- lcc_faqs table exists with correct schema, FK, RLS, and index
- lcc-photos Storage bucket exists with public=true
- Integration tests verify all four requirements
</success_criteria>

<output>
After completion, create `.planning/phases/06-website-infrastructure/06-01-SUMMARY.md`
</output>
