---
phase: 03-lcc-dashboard
plan: 05
type: execute
wave: 5
depends_on: [03-01, 03-02, 03-03, 03-04]
files_modified:
  - tests/e2e/lcc-dashboard/automations.spec.ts
autonomous: true
requirements: [DASH-05]
gap_closure: true

must_haves:
  truths:
    - "DASH-05 E2E test navigates to a lead detail page before asserting automations-section is visible"
    - "DASH-05 E2E test passes without timeout — automations-section exists on the page under test"
    - "Webhook status assertion checks text content ('Webhook configured' or 'Not configured'), not [data-testid] child count"
  artifacts:
    - path: "tests/e2e/lcc-dashboard/automations.spec.ts"
      provides: "Corrected DASH-05 E2E test"
      contains: "getByTestId('lead-card')"
  key_links:
    - from: "tests/e2e/lcc-dashboard/automations.spec.ts"
      to: "app/(lcc)/lcc/dashboard/leads/[id]/page.tsx"
      via: "click lead-card → navigate to /lcc/dashboard/leads/[id]"
      pattern: "lead-card.*click|click.*lead-card"
    - from: "automations-section assertion"
      to: "webhook status text"
      via: "getByText with regex /Webhook configured|Not configured/"
      pattern: "Webhook configured|Not configured"
---

<objective>
Fix the broken DASH-05 E2E test in automations.spec.ts.

Purpose: The Wave 0 stub test was never updated after Plan 03 placed automations-section on the lead detail page rather than the dashboard. The test logs in, lands on /lcc/dashboard, and immediately queries automations-section — which does not exist there. The test also asserts [data-testid] child count >= 1 inside automations-section, but the webhook status rows have no data-testid attributes in the implementation. Both assertions are structurally broken. The production code is correct; only the test needs to change.
Output: tests/e2e/lcc-dashboard/automations.spec.ts rewritten to navigate to a lead detail page and assert on text content.
</objective>

<execution_context>
@/Users/eliahshansult/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eliahshansult/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-lcc-dashboard/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix automations.spec.ts — navigate to lead detail page and assert text content</name>
  <files>tests/e2e/lcc-dashboard/automations.spec.ts</files>
  <action>
    Rewrite tests/e2e/lcc-dashboard/automations.spec.ts with the following corrected test body:

    1. Keep the loginAsLcc1 helper exactly as-is (goto /login, fill email/password, click submit, expect URL /lcc/dashboard).

    2. After loginAsLcc1(page), add these steps before any automations-section assertion:
       - Wait for at least one lead card to be visible: await expect(page.getByTestId('lead-card').first()).toBeVisible()
       - Click the first lead card: await page.getByTestId('lead-card').first().click()
       - Wait for the URL to include '/lcc/dashboard/leads/': await expect(page).toHaveURL(/\/lcc\/dashboard\/leads\//, { timeout: 10_000 })

    3. Then assert automations-section is visible — this is unchanged:
       const automationsSection = page.getByTestId('automations-section')
       await expect(automationsSection).toBeVisible()

    4. Replace the [data-testid] child count assertion with a text content assertion:
       - Remove the `children` locator and `count` assertion entirely.
       - Instead, assert that automationsSection contains the text "Webhook configured" OR "Not configured" using a regex:
         await expect(automationsSection).toContainText(/Webhook configured|Not configured/)

    The test description and describe block name must stay the same: 'Automations Section (DASH-05)' and 'DASH-05: automations section is visible with at least one webhook status indicator'.

    Remove the Wave 0 comment block (lines 3-5) since the test is now active and correct.

    Do NOT add data-testid attributes to the production implementation — the gap analysis confirmed the fix should be in the test only.
  </action>
  <verify>
    <automated>cd "/Users/eliahshansult/AI Work/Claude Code/lcc-lead-engine" && npx tsc --noEmit --project tsconfig.json 2>&1 | grep automations || echo "no type errors in automations.spec.ts"</automated>
  </verify>
  <done>
    automations.spec.ts navigates to a lead detail page via lead-card click before querying automations-section; the text assertion uses /Webhook configured|Not configured/ regex; TypeScript compiles without errors in the test file; the [data-testid] child count assertion is gone.
  </done>
</task>

</tasks>

<verification>
Confirm the final automations.spec.ts:
- Contains `page.getByTestId('lead-card').first().click()` before any automations-section query
- Contains `toHaveURL(/\/lcc\/dashboard\/leads\//)` wait after the click
- Contains `toContainText(/Webhook configured|Not configured/)` assertion
- Does NOT contain `locator('[data-testid]')` or `children.count()`
- Does NOT contain any `test.skip` wrapper
- TypeScript compiles clean: `npx tsc --noEmit 2>&1 | grep automations` returns no errors
</verification>

<success_criteria>
- automations.spec.ts no longer queries automations-section on /lcc/dashboard
- Test navigates to lead detail page (click lead-card → wait for /lcc/dashboard/leads/ URL)
- Webhook status assertion uses text content check, not [data-testid] child count
- DASH-05 truth "LCC can see which automations are active" is testable via this spec when run against a local Supabase instance with seeded lcc1 data
</success_criteria>

<output>
After completion, create `.planning/phases/03-lcc-dashboard/03-lcc-dashboard-05-SUMMARY.md` documenting:
- What changed in automations.spec.ts (navigation fix + assertion fix)
- Why: test was written as Wave 0 stub before implementation placed automations-section on lead detail page
- The two specific bugs closed: wrong page navigation, [data-testid] child count vs text content
</output>
