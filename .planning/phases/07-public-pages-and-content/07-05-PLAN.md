---
phase: 07-public-pages-and-content
plan: 05
type: execute
wave: 2
depends_on: [07-04]
files_modified:
  - app/[lccSlug]/layout.tsx
autonomous: true
requirements: [PAGE-02, PAGE-03, PAGE-04, PAGE-05]
gap_closure: true

must_haves:
  truths:
    - "Sub-pages /about, /au-pairs, /faq, and /testimonials display a cream/light background (bg-brand-pageBg) matching the landing page"
    - "The fix applies to all sub-pages without touching each page file individually"
  artifacts:
    - path: "app/[lccSlug]/layout.tsx"
      provides: "Shared layout wrapper with bg-brand-pageBg min-h-screen on the <main> element"
      contains: "bg-brand-pageBg min-h-screen"
  key_links:
    - from: "app/[lccSlug]/layout.tsx"
      to: "app/[lccSlug]/about/page.tsx (and other sub-pages)"
      via: "Next.js layout children prop"
      pattern: "bg-brand-pageBg"
---

<objective>
Fix the dark background on all LCC sub-pages by adding bg-brand-pageBg to the shared layout's <main> element.

Purpose: The UAT identified that /about, /au-pairs, /faq, and /testimonials render with a black body background because globals.css sets --background: #0a0a0a in dark mode via prefers-color-scheme. The landing page avoids this because every section has explicit bg-brand-pageBg. Sub-pages have no background class on their root divs.
Output: A single one-line change to app/[lccSlug]/layout.tsx that fixes all four sub-pages at once.
</objective>

<execution_context>
@/Users/eliahshansult/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eliahshansult/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-public-pages-and-content/07-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bg-brand-pageBg to the shared lccSlug layout main element</name>
  <files>app/[lccSlug]/layout.tsx</files>
  <action>
In app/[lccSlug]/layout.tsx, change the <main> element from:

  <main>{children}</main>

to:

  <main className="bg-brand-pageBg min-h-screen">{children}</main>

This applies the cream background to every sub-page rendered under the [lccSlug] layout. The landing page (page.tsx) is unaffected — it already uses explicit bg-brand-pageBg on each section div and does not rely on the main wrapper color.

Do NOT add bg-brand-pageBg to the fragment wrapper or to individual sub-page files. The single change to layout.tsx is the entire fix.
  </action>
  <verify>
    <automated>cd "/Users/eliahshansult/AI Work/Claude Code/lcc-lead-engine" && grep -n "bg-brand-pageBg" app/\[lccSlug\]/layout.tsx</automated>
  </verify>
  <done>app/[lccSlug]/layout.tsx contains bg-brand-pageBg on the main element; no sub-page files were modified</done>
</task>

</tasks>

<verification>
1. Run: grep -n "bg-brand-pageBg" app/[lccSlug]/layout.tsx — must show the main element line
2. Run: grep -rn "bg-brand-pageBg" app/[lccSlug]/about/page.tsx app/[lccSlug]/au-pairs/page.tsx app/[lccSlug]/faq/page.tsx app/[lccSlug]/testimonials/page.tsx — must return zero matches (sub-pages untouched)
3. Manual smoke check: visit /kim-johnson/about — background should be cream, not black
</verification>

<success_criteria>
All four sub-pages (/about, /au-pairs, /faq, /testimonials) render with a cream background matching the landing page. The fix is isolated to a single line in layout.tsx.
</success_criteria>

<output>
After completion, create .planning/phases/07-public-pages-and-content/07-05-SUMMARY.md
</output>
